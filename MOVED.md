# gpt-bot has moved

This repository is archived. The bot now lives in the `discord-bot` monorepo as `bots/gpt`, together with the other Discord bots and the modules they share. Its full history was imported there with `git subtree`; the last commit here is tagged `pre-monorepo-20260921`.
